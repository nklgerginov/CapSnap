# Whisper Transcription Service (FastAPI)

This service provides a /api/transcribe/whisper endpoint that returns word-level subtitle blocks.

This scaffold includes a working FastAPI app, request/response schemas, a lightweight Transcriber wrapper (with a dummy fallback) and tests.

See README.md for usage.
